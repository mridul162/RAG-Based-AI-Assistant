# Project Flow

1. Created project root.

2. Created virtual environment:

```powershell id="jlwn44"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

3. Installed initial dependencies:

```powershell id="jlwn47"
pip install pyyaml python-slugify
```

4. Built `template.py`.

5. Generated full project architecture:

```powershell id="jlwn4a"
python template.py
```

6. Created engineering docs:

```text id="jlwn4d"
PROJECT_CONTEXT.md
SYSTEM_ARCHITECTURE.md
DECISIONS_LOG.md
KB_DESIGN.md
CHUNKING_STRATEGY.md
```

7. Defined:

   * retrieval-first architecture
   * semantic isolation strategy
   * incremental indexing philosophy
   * partial re-embedding strategy
   * heading-aware chunking strategy

8. Designed canonical product KB structure.

9. Built `create_product_template.py`.

10. Generated first product KB:

```powershell id="jlwn4g"
python product_template.py --category honey --product sundarbans_kholisha_honey
```

11. Built bulk generation script:

```text id="jlwn4j"
scripts/generate_products.ps1
```

12. Executed bulk product KB generation:

```powershell id="jlwn4m"
.\scripts\generate_products.ps1
```

13. Designed metadata architecture:

```text id="jlwn4p"
product_schema.json
chunk_schema.json
metadata_schema.json
retrieval_schema.json
```

14. Standardized `product.yaml` schema.

15. Implemented metadata enrichment strategy:

* filesystem metadata
* product metadata
* retrieval metadata
* multilingual retrieval support

16. Created ingestion architecture:

```text id="jlwn4s"
ingestion/
│
├── loaders/
│   ├── markdown_loader.py
│   └── product_metadata_loader.py
│
├── parsers/
│   └── markdown_parser.py
│
├── validators/
│   └── kb_validator.py
│
├── chunkers/
│   └── semantic_chunker.py
│
├── embedders/
│   └── bge_embedder.py
│
├── vectorstores/
│   └── faiss_store.py
│
├── pipelines/
│   │   └── ingestion_pipeline.py
│
└── utils/
    ├── normalizer.py
    └── logging_utils.py
```

17. Added package initialization files:

```powershell id="jlwn4v"
New-Item ingestion\__init__.py
New-Item ingestion\loaders\__init__.py
New-Item ingestion\parsers\__init__.py
New-Item ingestion\validators\__init__.py
New-Item ingestion\chunkers\__init__.py
New-Item ingestion\embedders\__init__.py
New-Item ingestion\vectorstores\__init__.py
New-Item ingestion\pipelines\__init__.py
New-Item ingestion\utils\__init__.py
```

18. Finalized artifact structure:

```text id="jlwn4y"
artifacts/
│
├── raw/
├── cleaned/
├── normalized/
├── chunked/
├── embeddings/
└── pipeline_logs/
```

19. Implemented:

* `markdown_loader.py`
* `product_metadata_loader.py`
* `kb_validator.py`
* `ingestion_pipeline.py`

20. Standardized UTF-8 ingestion handling for:

* markdown
* YAML
* multilingual metadata
* Bangla text support

21. Standardized module-based execution:

```powershell id="jlwn51"
python -m ingestion.pipelines.ingestion_pipeline
```

22. Implemented semantic markdown parser:
        heading-aware parsing
        HTML <section> preservation
        markdown table preservation
        HTML table preservation
        heading hierarchy tracking
23. Implemented multilingual-safe normalization:
        Unicode normalization
        whitespace normalization
        line-ending normalization
        structure-preserving cleanup
24. Implemented semantic chunking system:
        section-aware chunking
        paragraph grouping
        semantic overlap
        metadata-rich chunks
        table-preserving chunking
        HTML table atomic preservation
25. Fixed semantic chunking issues:
        HTML table fragmentation
        overlap duplication instability
        semantic block preservation
26. Implemented artifact persistence system:
        parsed artifacts
        normalized artifacts
        chunk artifacts
        pipeline execution logs
27. Integrated full ingestion orchestration pipeline:
        Validate KB
            ↓
        Load Markdown Documents
            ↓
        Parse Semantic Sections
            ↓
        Normalize Content
            ↓
        Generate Semantic Chunks
            ↓
        Persist Artifacts
            ↓
        Generate Pipeline Summary
28. Current Pipeline Outputs:
        artifacts/parsed/
        artifacts/normalized/
        artifacts/chunked/
        artifacts/pipeline_logs/

29. Build and Implemented bge_embedder with `python -m ingestion.embedders.bge_embedder1` command.
